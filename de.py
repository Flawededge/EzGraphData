import matplotlib.pyplot as plt

plt.show()


def pl(data):
    plt.cla()
    if not isinstance(object, (list,)):
        data = [data]
    for i in data:
        plt.plot(i)


aa = ((20 * p * mainData.cap[i]) / (20 * -cur.where(cur < 0)))
time = mainData.plotData['deltaT']
(time / (aa ** mainData.peu[i])).fillna(0)
